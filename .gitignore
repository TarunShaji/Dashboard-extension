# ── Reference-only files (not part of the extension project) ──
db/
system-architecture.md

# ── Environment variables ──
.env

# ── Chrome extension private key ──
extension.pem
.env.local
.env.*.local

# ── Dependencies ──
node_modules/
email-extractor/node_modules/

# ── Bun ──
# bun.lock is intentionally committed for reproducible installs
# (remove this line if you want to ignore it)

# ── Build output ──
dist/
build/
out/

# ── macOS ──
.DS_Store
.AppleDouble
.LSOverride

# ── Editors ──
.vscode/
.idea/
*.swp
*.swo

# ── Debug output ──
email-extractor/outputs/

# ── Logs ──
*.log
npm-debug.log*
bun-debug.log*
